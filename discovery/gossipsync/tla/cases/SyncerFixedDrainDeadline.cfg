\* Must fail: NoCrossAttemptCredit. A drain deadline fixed at abandonment,
\* as before the drain timer became an inactivity timer, fires while a
\* peer that paused once is still answering.

SPECIFICATION Spec

CONSTANTS
    NumBlocks = 4
    MaxAttempts = 3
    MaxBatches = 2
    MaxReplies = 4
    MaxPauses = 1
    Stalls = TRUE
    MaxDups = 0
    MaxCorrupts = 0
    Format = "lnd"
    CompleteEveryReply = FALSE
    NoDraining = FALSE
    NoFirstReplyCheck = FALSE
    LegacyDrainOnLastBlock = FALSE
    FixedDrainDeadline = TRUE

INVARIANTS
    NoCrossAttemptCredit

\* Every behavior of the world is finite, so its last state has no
\* successor. That is the end of the run, not a deadlock.
CHECK_DEADLOCK FALSE
