\* Must fail, a finding: WholeStreamCredit. The production syncer with a
\* legacy-format peer slower than A-DRAIN. The second long pause ends the
\* drain partway through the stream, and the tail, whose legacy replies
\* skip the range checks, completes the next attempt on its own. SPEC.md
\* states GSS-013 for any timing, and P checks it only for lnd-format peers.

SPECIFICATION Spec

CONSTANTS
    NumBlocks = 4
    MaxAttempts = 3
    MaxBatches = 2
    MaxReplies = 4
    MaxPauses = 6
    Stalls = TRUE
    MaxDups = 0
    MaxCorrupts = 0
    Format = "legacy"
    CompleteEveryReply = FALSE
    NoDraining = FALSE
    NoFirstReplyCheck = FALSE
    LegacyDrainOnLastBlock = FALSE
    FixedDrainDeadline = FALSE

INVARIANTS
    WholeStreamCredit

\* Every behavior of the world is finite, so its last state has no
\* successor. That is the end of the run, not a deadlock.
CHECK_DEADLOCK FALSE
