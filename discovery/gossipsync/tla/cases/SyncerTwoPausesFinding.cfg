\* Must fail, a finding: NoCrossAttemptCredit. The production syncer with a
\* peer that pauses past the timeout twice in one stream. The first pause
\* fails the attempt, the second fires the drain timer, and the stream
\* reaches the next attempt. This is open question Q9: A-DRAIN is needed.

SPECIFICATION Spec

CONSTANTS
    NumBlocks = 4
    MaxAttempts = 3
    MaxBatches = 2
    MaxReplies = 4
    MaxPauses = 2
    Stalls = TRUE
    MaxDups = 0
    MaxCorrupts = 0
    Format = "lnd"
    CompleteEveryReply = FALSE
    NoDraining = FALSE
    NoFirstReplyCheck = FALSE
    LegacyDrainOnLastBlock = FALSE
    FixedDrainDeadline = FALSE

INVARIANTS
    NoCrossAttemptCredit

\* Every behavior of the world is finite, so its last state has no
\* successor. That is the end of the run, not a deadlock.
CHECK_DEADLOCK FALSE
